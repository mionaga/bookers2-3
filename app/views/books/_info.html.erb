<!--@user → user -->

     <h1>User info</h1>

      <table class="table">
        <tbody>
          <tr><%= image_tag user.get_profile_image(100,100) %></tr>
          <tr>
            <th>name</th>
            <th>introduction</th>
          </tr>
          <tr>
          <td><%= user.name %></td>
          <td><%= user.introduction %></td>
          </tr>
        </tbody>
      </table>

      <div class="btn btn-outline-secondary btn-block edit_user_135206">
        <%= link_to edit_user_path(user.id) do %>
          <i class="fa-solid fa-user-gear"></i>
        <% end %>
      </div>
